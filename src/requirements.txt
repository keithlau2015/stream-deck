# StreamDeck Dependencies
# Core GUI and Graphics
pygame>=2.1.0

# System Tray Support
pystray>=0.19.0

# Image Processing (required by pystray)
Pillow>=8.0.0

# Clipboard Operations
pyperclip>=1.8.0

# Serial Communication (Arduino)
pyserial>=3.5

# Build tool
pyinstaller>=5.0.0

# Auto-update system dependencies
requests>=2.25.0
packaging>=21.0.0

# Additional utilities that may be needed
# Note: webbrowser, subprocess, json, os, sys, time, ctypes, threading
# are part of Python standard library and don't need to be installed
# 
# IMPORTANT: tkinter is part of Python standard library but may not be included
# in some Python distributions (especially embedded/minimal installations).
# If you get "ModuleNotFoundError: No module named 'tkinter'", you may need to:
# 1. Install tkinter separately: pip install tk
# 2. Or reinstall Python with tkinter support
# 3. Or use a different Python distribution that includes tkinter
